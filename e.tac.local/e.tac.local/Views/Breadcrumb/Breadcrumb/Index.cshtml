@model IEnumerable<e.tac.local.Models.NavigationItem>
    <!-- [Breadcrumb Navigation] -->


@*<ol class="breadcrumb">
    @foreach (var item in Model)
    {
        <li class="@(item.Active ? "active" : string.Empty)">
            <a href="@item.URL">@item.Title</a>
        </li>
    }
</ol>*@
<ol class="breadcrumb">

    @foreach (var item in Model)
    {
    <li>
    <a class="@(item.Active ? "active" : string.Empty)"
       href="@item.Url">
        @item.Title
        </a>
    </li>
    }
</ol>
<!-- [/Breadcrumb Navigation] -->
